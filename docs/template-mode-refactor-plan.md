# 模板模式改造与 Bug 修复计划

## 目标

把现有应用整理为三块独立体验：

1. 生图模式
2. 模板模式
3. 收藏夹

同时修复当前几个关键问题：

- 创建模板时，不应再在普通生图画廊里多出一张模板卡
- 模板多选后，要能真正批量复用
- 选择模板后，需要明确提示导入额外参考图 / 材质图
- 卡片提示词显示要改成单行省略号
- 模板标签颜色功能要恢复可用

## 现状判断

项目里已经有一些底座，但还没串成完整流程：

- `TaskRecord.kind = 'template'` 已存在
- `templatePickerTaskIds / openTemplatePicker / closeTemplatePicker` 已存在
- 收藏夹、ZIP 导入导出、任务体系都已存在
- 普通任务列表已经尝试过滤模板，但展示和保存链路仍然串线
- `Agent` 仍然是主入口之一，需要从主导航中移除

## 最终产品形态

### 1. 生图模式

原来的“画廊”改名为“生图模式”。

职责：

- 普通图片生成
- 任务列表浏览
- 单任务复用 / 删除 / 收藏

要求：

- 不显示模板卡
- 不和模板模式混在一起

### 2. 模板模式

新增独立入口“模板模式”。

职责：

- 浏览模板卡
- 多选模板
- 批量复用模板
- 从当前输入保存模板

要求：

- 模板卡只在模板模式中出现
- 支持单槽模板兼容旧数据
- 支持多参考图 / 多替换位
- 当前实现：模板保存与模板编辑都支持多选替换位；批量套用时会按选中模板的替换位数量展示上传槽。

### 3. 收藏夹

收藏夹保持独立入口。

要求：

- 继续支持收藏任务
- 继续支持默认收藏夹
- 不和模板模式合并视图

## 需要做的改造

### 导航与模式

- 删除 `Agent` 作为主导航入口
- 将原 `gallery` 改名为 `生图模式`
- 新增 `模板模式`
- 收藏夹保留独立入口

### 模板保存

从当前输入区保存模板时，需要支持：

- 提示词
- 1 张或多张参考图
- 指定哪些图片位是可替换位
- 可一次选择多张参考图作为替换位，替换位下标保存到 `templateReplaceImageIndexes`

默认规则：

- 仍沿用旧习惯，最后一张图作为替换位
- 兼容旧模板数据：旧字段 `templateReplaceImageIndex` 保留并继续写入第一个替换位；读取时统一走 `getTemplateReplaceImageIndexes()`

### 模板批量复用

流程应为：

1. 在模板模式中多选模板
2. 点击批量复用
3. 打开上传页面，按选中模板的替换位数量导入额外参考图 / 材质图
4. 导入后开始批量生成

生成规则：

- 按模板保存时的提示词、参考图、替换位规则逐个复用
- 单替换模板使用“单张参考图上传”
- 多替换模板使用“多张参考图上传 1/2/3...”并按替换位下标顺序替换
- 单替换模板与多替换模板混选时，上传页面同时显示单张上传和多张上传槽
- 批量创建普通生图任务

### 展示与筛选

- 普通生图列表不显示模板卡
- 收藏夹视图只展示真实收藏内容
- 模板模式中才显示模板卡和模板批量操作

### 卡片 UI

- 提示词超出一行时，直接显示单行省略号
- 不要中间截断
- 模板卡和普通任务卡视觉上明确区分

### 标签颜色

- 恢复模板 / 任务卡的自定义标签颜色
- 确保颜色真正参与渲染
- 刷新与导入导出后仍能保持

## 建议实现顺序

### 第一阶段

- 新增模板模式入口
- 移除 Agent 主导航入口
- 将画廊命名改成生图模式
- 保留收藏夹独立入口

### 第二阶段

- 修复模板保存不污染普通画廊
- 修复模板卡误混入收藏夹 / 普通任务列表
- 让模板卡只在模板模式展示

### 第三阶段

- 接通模板多选后的批量复用流程
- 增加“导入额外参考图 / 材质图”的提示
- 让批量生成真正落到任务创建链路

### 第四阶段

- 修复提示词单行省略号
- 修复标签颜色持久化与渲染
- 做导入导出回归

## 验收标准

- 创建模板后，普通生图画廊不再多出模板卡
- 模板模式里可以看到模板卡
- 收藏夹和模板模式互不串台
- 模板多选后可以进入批量复用流程
- 会提示并展示需要导入的额外参考图 / 材质图上传槽
- 1 个替换位显示 1 个上传框，2 个显示 2 个，3 个显示 3 个；混选单/多替换模板时同时出现单图与多图上传区
- 导入后可以批量生成
- 提示词卡片显示为单行省略号
- 标签颜色可正常生效并持久化
- 旧 ZIP 导入导出仍可正常工作

## 备注

- 继续复用现有任务体系和 ZIP 结构，不新增独立模板包
- Agent 数据可以保留兼容，但不作为主入口；生图筛选和设置页不再展示 Agent 专属入口/选项
- 先保证功能可用，再做交互细化
